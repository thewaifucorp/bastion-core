# Loop autônomo — relatório de execução e findings

> Corrida iniciada 2026-07-13. Itens que exigem olho do owner ou follow-up marcados por seção.

## Findings de review (não bloqueantes, endereçados no fluxo)

1. ~~**[M3] `ToolSource::call_tool_with_timeout` é ungated por construção.**~~ **RESOLVIDO (M3-01).** Os dois call sites pré-existentes de bypass do registry (fallback de registry vazio em `dispatch_tool_loop` e o tool-loop inteiro de `run_provider_fallback`) aplicavam `check_egress` manualmente ANTES de chamar (WR-02/D-13). O trait `ToolSource::call_tool_with_timeout` (`crates/bastion-runtime/src/agent/ports.rs`) agora recebe `resolved_tier: Option<PrivacyTier>` e a implementação de produção (`McpToolSource`, `crates/bastion-mcp/src/tool_source.rs`) chama `check_egress` internamente ANTES de despachar — os dois call sites só passam o tier adiante, não chamam `check_egress` mais. Comportamento idêntico (mesma checagem, mesmo chokepoint lógico), agora inesquecível por construção. Coberto por teste de invariante novo (`tests/characterization_boundary.rs::tool_source_gate_blocks_dispatch_on_local_only_tier` + `::mcp_tool_source_gates_egress_before_attempting_dispatch`, mapeado em `docs/revamp/M1-07-characterization-map.md` linha "F1"). Trust-tagging paridade (o gap de `TaggedValue` nesses paths de bypass) permanece pré-existente e fora de escopo desta correção — não endereçado aqui.
2. **[3b] `GoalPort` retornava `crate::goal::Goal` na assinatura do kernel** — vazamento de tipo de cognição. Resolvido no 3b movendo `Goal` (e `PrivacyTier`) pra `bastion-types`.
3. ~~**[M3→M4] Furos de API de approval descobertos pelo `embedded-host` (M3-CLOSE.md §3):** (a) `AgentLoop::new` hardwira `ApprovalQueue::new(db_path)` — host não injeta política própria; (b) `ApprovalQueue` é struct SQLite concreta, não port (M1-04 previa "ports opcionais de approval"); (c) rejeição invisível — `Rejected` mapeia pra `AlreadyPending`, re-invocação retorna `Ok({awaiting_approval:true})` em vez de Err tipado (assimetria com `PrivacyEgressBlocked`). O (c) é mudança de comportamento observável — desenhar com cuidado (Err tipado `ApprovalDenied`), executar antes do M5. O exemplo tem asserts que quebram quando isso for corrigido — atualizar junto.~~ **RESOLVIDO (Ciclo 2.1).** `docs/revamp/C2-approval-port-design.md`: (a)/(b) fechados — `ApprovalGate` trait (`crates/bastion-runtime/src/agent/ports.rs`) + `SqliteApprovalGate`/`NullApprovalGate` (`capability/approval.rs`); `AgentLoop::new` recebe `Arc<dyn ApprovalGate>` injetado, nunca mais hardwira SQLite. (c) fechado com a mudança de comportamento deliberada: `ApprovalOutcome::Rejected(DenyScope)` + `BastionError::ApprovalDenied{capability, scope}` (`bastion-types`), consumido uma vez em `outcome_for_existing_row` (nunca loop eterno de Err, auditoria preservada). `examples/embedded-host/src/main.rs` atualizado — injeta `ThresholdDenyGate` próprio (sem SQLite) e observa o `Err` tipado. Mapeado em `docs/revamp/M1-07-characterization-map.md` linha "4b"; testes novos em `crates/bastion-runtime/src/capability/approval.rs::tests`, `tests/agent_loop_public.rs::turn_scoped_denial_skips_remaining_tool_calls_and_ends_turn` (critério de aceite #2 — segunda tool nunca executa), `tests/session_continuity.rs` (fix do bug latente de ordering Tool-após-Tool que o teste acima expôs).
4. ~~**[pré-existente, M3+] Paridade de trust-tagging nos paths de bypass do ToolSource** — bypass egress-gated mas resultados não recebem `TaggedValue` untrusted como `registry.invoke` aplica. Avaliar junto do fix de approval.~~ **RESOLVIDO (Ciclo 2.1).** `docs/revamp/C2-approval-port-design.md` §4: `TaggedValue::untrusted(source, data)` (`capability/registry.rs`) expõe a wrapping do registry; `tag_bypass_result`/`frame_tool_result_content` (`agent/loop_.rs`) compartilhados pelos dois call sites de bypass (`dispatch_tool_loop`'s empty-registry fallback + `run_provider_fallback`'s tool loop inteiro, que antes não aplicava NENHUMA tag) — nunca duplicado. Coberto por `tests/characterization_boundary.rs::bypass_tag_matches_registry_tag_for_equivalent_non_local_result`.
5. ~~**[A-05 → revisão do contrato A-01] 6 furos achados na validação live dos adapters** (detalhe em `docs/revamp/A-05-conformance-matrix.md` §5): (1) `WATCHDOG` 5s da conformance é apertado pra harness cloud frio — parametrizar; (2) sandbox degrada silenciosamente sem bubblewrap — `SandboxCoverage` precisa ser detectado, não declarado estático; (3) race de readiness no `turn/steer` do Codex (mitigado com retry bounded no adapter); (4) `turn/interrupt` ambíguo cancel/timeout (mitigado com tracking client-side); (5) **SEGURANÇA (T4-adjacente):** `respond_permission(Deny)` gateia UMA tool-call — modelo capaz contorna por tool alternativa não-gateada; deny precisa poder escalar pra cancel-turn ou deny-classe, decisão de design pro M4/BackendProfile; (6) `resume()` não recebe `SessionSpec` — política env/timeout/permission não é recuperável no reattach, adapter usa defaults conservadores; considerar `ResumeSpec` no contrato.~~ **RESOLVIDO (Ciclo 2.2).** `docs/revamp/A-01-agentruntime-contract.md` (nota "v2" no topo) + `docs/revamp/A-05-conformance-matrix.md` §7 (re-run). (1) `ConformanceScenarios::watchdog: Duration` substitui o `const WATCHDOG` — live passa 30s. (2) `SandboxCoverage` agora é DETECTADO (`CodexAppServerRuntime::health()`/`start()` rodam `probe_sandbox_coverage`, probe real de `bwrap --unshare-user`; sem bubblewrap funcional → `None`, nunca `Partial` otimista — confirmado live: `None` neste host). (3)/(4) já mitigados nos adapters, agora formalizados como requisito do contrato (`RuntimeSession::steer`/`RuntimeEvent::Ended` rustdoc, A-01 §5 itens 3/5). (6) `AgentRuntime::resume(&self, handle, spec: ResumeSpec)` — env/timeout re-aplicados de verdade; `permissions` não cabe no protocolo do Codex (`thread/resume` só aceita `threadId`), surfaced como `Warning` — verificado live (`docs/revamp/A-05-conformance-matrix.md` §7.2). **(5) — deny-escalation, ver subitem abaixo.**
   - **(5) SEGURANÇA (T4-adjacente) — RESOLVIDO no design pelo Ciclo 2.1, adapter alinhado no Ciclo 2.2.** Decisão de escopo do deny já fechada em `docs/revamp/C2-approval-port-design.md` §3 (`DenyScope::Instance`/`Turn`, default `Turn`). Ciclo 2.2 alinha o contrato `AgentRuntime`: `PermissionDecision::Deny` agora carrega o mesmo `DenyScope` (espelho local documentado — `bastion-agent-runtime` não depende de `bastion-types`); com `Turn`, o adapter cancela a task graciosamente após negar, fechando o gap de roteamento por tool alternativa também no lado do adapter, não só no kernel. Testado no fake (`deny_turn_scope_cancels_the_task`, `tests/agent_runtime_conformance.rs`) e implementado em `CodexAppServerRuntime::respond_permission` (`crates/bastion-agent-runtime/src/codex.rs`).
6. **[Ciclo 2.4 — achados na prática, não bloqueantes]** Quatro furos/impasses reais descobertos integrando `BackendProfile`/modo 2/modo 3 e rodando A-06/A-07 ao vivo (detalhe completo em `docs/revamp/A-06-A-07-live.md`):
   - ~~**(a) Aprovação síncrona não cabe no modelo de turn de hoje.**~~ **RESOLVIDO (Loop 3-A, `docs/revamp/C3-runtime-followups-design.md` §6a).** Fila assíncrona persistida `PermissionGate`/`permission_queue` (owner-scoped, correlacionada por `PendingPermission::row_id` — não pelo `PermissionRequestId` do harness, que só é único DENTRO de uma sessão). O consumidor de mode 3 (`spawn_delegated_task_consumer`, um tokio task independente que nunca segura `&mut agent`) agora PAUSA de verdade (`wait_for_permission_resolution`) até: uma decisão explícita de um turno posterior (`AgentLoop::respond_permission`), o timeout (`expires_at` → `Deny{Turn}` fail-closed, registrado na fila), ou a task ser cancelada enquanto pausada (idem). Mode 2 (`run_runtime_backed_turn`) permanece deny-imediato por design — pausar ali seguraria `&mut agent` e congelaria o daemon pra todos os outros owners; documentado no rustdoc de ambas as funções. Testado completo no FakeRuntime (`tests/agent_runtime_cross_turn_permission.rs`, 3 cenários: allow/deny explícito/timeout) + 6 testes unitários do gate. Matriz de suporte por adapter (honesta): nem Codex nem acpx têm pausa longa comprovada em produção — a infra funciona plenamente com qualquer harness que exponha isso; nos dois adapters shipped hoje, o comportamento efetivo permanece `Deny{Turn}` no timeout configurado (10min default), não uma pausa indefinida real-world verificada.
   - **(b) `AgentRuntime::resume` não cobre task-in-flight cross-restart.** Nem `codex_app_server` nem `acpx` fazem replay/buffer de eventos de uma task que já estava rodando quando a conexão caiu — `resume()` reata a SESSÃO, não a TASK. `resume_delegated_task` é honesto sobre isso (submete uma nova task de follow-up) — mas isso significa que hoje **não existe** "a mesma tarefa continua de onde parou" através de um restart real; só existe "a mesma sessão pode receber uma tarefa nova". Se o produto algum dia precisar de continuidade de task genuína, é um furo de protocolo a resolver com os mantenedores do harness, não algo que Bastion pode simular sozinho. **Fora de escopo do Loop 3-A** — furo de protocolo do harness, não um gap do kernel.
   - ~~**(c) `scripts/dump-public-api.sh` tem um ponto cego pré-existente: `pub async fn`.**~~ **RESOLVIDO (Loop 3-A, commit 6c).** Regex ampliado pra `pub\s+(?:(?:async|unsafe|const)\s+)*(fn|...)` — 90 itens `pub async fn` previamente invisíveis apareceram nos baselines regenerados (7 crates: bastion-runtime +30, bastion-cognition +17, bastion-agent-runtime +15, bastion +12, bastion-mcp +9, bastion-mesh +4, bastion-personas +3). Não é regressão — a superfície já existia, só ficou visível ao gate `--check`.
   - ~~**(d) `pending_tx`/`pending_rx` (seam PROACT-05) não é owner-roteado.**~~ **RESOLVIDO (Loop 3-A, `docs/revamp/C3-runtime-followups-design.md` §6d).** Item enfileirado agora é `PendingItem { owner: Option<String>, text: String }`; `spawn_delegated_task_consumer` e `CronService::run_heartbeat`/`on_event` tageiam com o owner real; consumidor (`main.rs`'s `pending_rx` arm) roteia via `run_turn_for(&item.text, owner)`, com `DEFAULT_OWNER` só como fallback pra item sem owner (nenhum produtor atual deixa de nomear um). Testado (`delegate_task_pending_item_never_crosses_owner_boundary`, `crates/bastion-runtime/src/agent/loop_.rs`): dois owners delegam tasks concorrentes no MESMO `AgentLoop`, cada resultado chega tageado com o owner correto, nunca cruzado.

## Encerramento do Loop 3-F — ÚLTIMO CHUNK do Loop 3 (2026-07-14)

Cinco commits sobre `origin/revamp`, fechando os 2 findings do 3-E, A-08, A-09 (etapa flag) e o M6-prep. Este é o **fechamento do Loop 3**: o workspace fica logicamente separável e pronto pro split físico que o OWNER executa (nenhum repo criado aqui).

- **`fix(c3): correlate invoke_agent span conversation.id to resolved per-owner session`** (`9368df2`) — finding #2 do 3-E (bug real). `AgentLoop::run_turn_for_with_trust` (`crates/bastion-runtime/src/agent/loop_.rs`) não estampa mais `gen_ai.conversation.id` na construção do span (que usava `self.session_id` de construção); agora seta via `turn_span.set_attribute(...)` com o `session_id` resolvido por-owner (CR-04) logo após a resolução — mesmo padrão pós-routing do `gen_ai.agent.name`. `examples/embedded-host-slice::demonstrate_otel_correlation` virou assertion POSITIVA: **2 owners → 2 `gen_ai.conversation.id` DISTINTOS**, cada um = a sessão real do seu owner (provado ao vivo, `cargo run` exit 0). Impact analysis antes (CRITICAL: superfície-contrato estável, 25 upstream) — mudança é aditiva de atributo, nenhum caller quebra.
- **`feat(c3): ContextBlock derives (Debug/Clone/PartialEq)`** (`9be9b9c`) — finding #1 do 3-E. `ContextBlock` (`crates/bastion-runtime/src/agent/context.rs`) agora deriva `Debug`/`Clone`/`PartialEq` — aditivo, um segundo consumidor consegue `assert_eq!`/logar blocos injetados via SEAM #2. `PrivacyTier` já derivava `PartialEq`, compila sem mais nada. Baseline pub inalterado (derives não entram na assinatura do dump).
- **`feat(a09): terminal-agent behind legacy-terminal-agent feature`** (`d6f1fdc`) — A-09 etapa flag. `TerminalAgentProvider` só compila sob `legacy-terminal-agent` (OFF por default). Build default não linka o legado + `resolve_provider("claude_code"|"opencode")` falha fail-closed com erro tipado apontando pro AgentRuntime; build com a feature ainda compila/resolve — **os dois estados verificados** (`cargo build` default OK; `cargo build -p bastion-providers --features legacy-terminal-agent` OK; testes dos dois estados). Código NÃO deletado, deprecação documentada.
- **`docs(a08): security invariants + live E2E scorecard + policy-coverage matrix`** (`6141d03`) — A-08. `docs/revamp/A-08-security-e2e.md`: **zero regressão** de invariante (egress/approval/trust/owner-isolation); suíte de invariantes rodada offline com scorecards; live token-free re-verificado (auth fail-closed com acpx real); matriz versionada 0.1.0 targets × auth × capabilities × coverage; não-executados marcados honestamente com motivo (token/mesh-docker/codex-via-acpx/sandbox-sem-bwrap/GPU-N/A).
- **`docs(m6-prep): separable-state map + owner split steps` (este commit)** — `docs/revamp/M6-PREP.md`: mapa Core (11 crates) vs Agent (app+skills+mobile+installer+docker+config), passo-a-passo do owner (git filter-repo/cópia), rewrite path→git→crates.io, decisões abertas (D-1/D-2/D-3), lista de limpeza pro owner. README ganhou seção "Core vs Agent"; AGENTS.md perdeu 3 refs `.planning/` mortas (detalhe em M6-PREP §5.4). Nenhum repo criado.

Gates verdes por commit: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --workspace` (**729 passed / 10 ignored** — o build default agora exclui 2 testes do módulo `terminal_agent` gated e adiciona 1 teste fail-closed, net -1 vs 730), `check-crate-deps.sh` PASS, `dump-public-api.sh --check` PASS, `check-scope-and-scrub.sh` PASS.

**Achados/impasses reportados (não escondidos):**
- **Topologia 11 crates ≠ "10+app" do M1-ADR/BACKLOG** — `bastion-extension-wasm` entrou no 3-C; tabelas de topologia deveriam dizer 11 (não-bloqueante, sinalizado em M6-PREP §1.1).
- **A-09 fica `[x]` só da etapa flag** — remoção plena ainda exige 1 release de deprecação + rollback testado end-to-end (gate do próprio A-09).
- **Disco crítico durante a corrida** (91-99%): host morreu 1x no meio do commit 3; recuperado do disco. ENOSPC remediado limpando `target/debug/incremental` + `.tmp*` órfãos + duplicatas de hash de binários de teste em `deps` (liberou ~20G no total).

### Follow-ups PRESERVADOS para ciclos futuros (não perder)

- **3-C:** ExtensionHost não fiado no `main.rs`/daemon de produção (sem comando `/extensions`); mecanismo `Subprocess` sem allowlist de env/segredos (filho recebe zero env); trust cripto real (`trust_tier_of` sempre `Local`, assinatura de publisher fica pro M4-13).
- **3-D:** `docker build` real não executado (rodar em CI com disco fresco); `POST /lifecycle/reload` não hot-swapa o registry do `PersonaResponder` (exigiria port novo no kernel); export `--with-identity` embute keypair age/Ed25519 em texto puro **by-design** (identidade portável, opt-in + chmod 0600).
- **3-B:** M4-13 discovery híbrido (catálogo de extensões) segue `[ ]`; adapter Cursor (ACP) inexistente; `codex` via `acpx` indisponível (mismatch de login-mode externo); `artifact_digest` do opencode (`FrameInterpreter` não reconhece o frame shape).
- **3-C/M4:** subagente + agente coletivo (M4-11) fora de escopo, segue pendente.
- **6b (Ciclo 2.4):** continuidade de task cross-restart é furo de protocolo do harness (fora de alcance do kernel).

## Encerramento do Loop 3-E (2026-07-14)

Dois commits sobre `origin/revamp`, fechando M5-01/M5-02/M5-06 (e, junto, M3-11 — mesmo contrato) do BACKLOG, seguindo `docs/revamp/C3-m5-second-consumer-design.md`: o segundo consumidor embedded-host de verdade, provando o boundary do M2 sob um SEGUNDO owner real, não só compilação (M3-04).

- **`feat(c3): M5.1 VersionedContextArtifact/ContextRevision contract`** — `crates/bastion-types/src/context_artifact.rs`. O M1-ADR já listava `VersionedContextArtifact`/`ContextRevision` na tabela de "APIs públicas mínimas", mas nenhum dos dois tipos existia em lugar nenhum do substrato antes deste commit (grep zero hits) — o contrato nasceu aqui: artefato opaco, versionado, append-only (`publish` rejeita versão não-monotônica — "rollback" é sempre uma revisão NOVA, nunca reescrita), com `provenance`, `effective_from` e um `StalePolicy` (`UseLastKnown`/`FailClosed`) declarado pelo artefato. Deliberadamente conservador: SÓ o tipo, nenhum port novo em `bastion-runtime` — publicação/fan-out/detecção de staleness ficam por conta do host (ver rustdoc do módulo). Vive em `bastion-types` (folha, zero deps) porque é dado puro.
- **`feat(c3): embedded-host-slice — M5 second consumer`** — `examples/embedded-host-slice/` (crate nova, expande `examples/embedded-host` do M3 em vez de substituí-lo — o M3 continua provando o seam mínimo; este prova os 7 componentes completos + M5.1 sob um segundo owner real). `Cargo.toml` só depende de `bastion-types`/`bastion-runtime`/`bastion-memory`/`bastion-personas` (path deps) + crates externos — **zero import de `bastion`, zero fork/patch de crate do substrato**, verificado manualmente e por `scripts/check-scope-and-scrub.sh`. Os 7 componentes, todos via API pública: (1) `AgentDefinition` — na prática `bastion_types::Persona` + `bastion_personas::persona::PersonaRegistry::new_from_map` (não existe um tipo `AgentDefinition` literal no substrato; `Persona` é o vocabulário real, e já é 100% construível fora de qualquer SOUL.md), servida ao host pelo MESMO `PersonaResponder` que o Agent pessoal usa — sem fork de schema; (2) contexto autoritativo por owner via `TurnContextProvider` (`HostObjectContextProvider`), cada owner só vê o seu próprio objeto; (3) capability dinâmica object-scoped (`approve_object:<id>`, nome computado por instância) via `CapabilityRegistry::register` público; (4) `ObjectPolicyDenyGate` — `ApprovalGate` próprio do host (regra de status de objeto, não numérica como o `ThresholdDenyGate` do M3) nega com `Err(BastionError::ApprovalDenied)` tipado; (5) dois owners num único `AgentLoop`: sessões CR-04 automaticamente separadas (`SessionManager::load_most_recent_id_for`), histórico sem cross-leak, contexto/RuleBundle nunca cruzam owner, e nenhuma entidade do host chega ao session store do Bastion (verificado por assert direto no histórico persistido); (6) OTel — ver achado abaixo; (7) trust/spotlighting: `TurnCapabilityScope::quarantine()` (a MESMA primitiva que `run_turn_for_with_trust(untrusted: true)` usa internamente) prova que toda capability fica genuinamente invisível durante uma janela untrusted e é restaurada por completo depois. M5.1 (`rule_bundle.rs`): `RuleStore` (owner→`VersionedContextArtifact`) + `FakeClock` (relógio host-controlado, evita sleep real) + `RuleBundleContextProvider` — os 7 passos do design doc provados end-to-end (ver placar abaixo), incluindo um segundo check ao vivo via `AgentLoop::build_system_prompt_parts` real (não só chamada direta ao provider).

**Achados de API (furos e um bug, documentados — não escondidos, não forçados a "verde" com gambiarra):**

1. **`ContextBlock` (`crates/bastion-runtime/src/agent/context.rs`) não deriva NADA** — nem `Debug`, nem `Clone`, nem `PartialEq`. Um segundo consumidor que queira `assert_eq!`/logar blocos injetados via SEAM #2 não consegue sem destructurar campo a campo manualmente (`examples/embedded-host-slice/src/rule_bundle.rs::only_content` existe só por causa disso). Fix sugerido, **não aplicado** (fora do escopo deste slice, risco baixo mas decisão do owner): `#[derive(Debug, Clone, PartialEq)]` — puramente aditivo, zero mudança de comportamento.
2. **BUG real, não apenas furo de API — `gen_ai.conversation.id` do span `invoke_agent` está errado para qualquer owner não-default.** `AgentLoop::run_turn_for_with_trust` (`crates/bastion-runtime/src/agent/loop_.rs`, ~linhas 1306-1326) estampa esse atributo com `self.session_id.clone()` (campo fixado na CONSTRUÇÃO do `AgentLoop`) — e só DEPOIS resolve a sessão real por-owner (CR-04, `load_most_recent_id_for`/`create_session_for`). Resultado observado ao vivo neste slice: os turns de `owner_a` e `owner_b` (dois owners DIFERENTES, sessões CR-04 DIFERENTES, comprovadas) produzem spans com o **MESMO** `gen_ai.conversation.id` — sempre o id da sessão de construção, nunca a sessão real de nenhum dos dois. Nenhum outro atributo do span identifica o owner. Efeito prático: um host multi-owner não pode correlacionar um span à sua própria sessão/objeto via NENHUM atributo hoje — só por ORDEM de chamada (funciona neste exemplo sequencial de processo único; não escala pra um host concorrente de verdade). `examples/embedded-host-slice/src/main.rs::demonstrate_otel_correlation` prova e documenta o bug em vez de mascará-lo (compara os dois spans contra o id de construção, não contra os ids reais). Fix sugerido, **não aplicado** (`loop_.rs` é superfície-contrato estável do AGENTS.md; mudar sua ordem de atribuição é uma mudança de comportamento observável, decisão do owner, não deste slice): mover o `KeyValue::new("gen_ai.conversation.id", ...)` pra DEPOIS da resolução de `session_id` por-owner, ou (melhor) estampar um atributo `owner`/`enduser.id` explícito além do conversation.id.
3. **Discrepância de numeração BACKLOG↔design doc (não bloqueante):** o BACKLOG chama este item de `M5-06`; `docs/revamp/C3-m5-second-consumer-design.md` chama a mesma entrega de "M5.1". Mesmo escopo, rótulos diferentes — sinalizado aqui em vez de escolhido por adivinhação (mesmo padrão já usado pro M4-13/M4-14-15 no Loop 3-D).
4. **Discrepância de contagem dentro do próprio design doc:** os critérios de aceite falam em "os 8 passos de propagação de regra", mas a lista numerada em §M5.1 tem exatamente 7 itens. Os 7 foram implementados e provados um a um (numerados 1-7, mapeando a lista do doc); nenhum 8º passo fantasma foi inventado pra bater a contagem.

**Placar dos 7 passos de propagação (M5.1/M5-06), todos `[OK]`:** 1-2) v1 publicado pra owner A, dois "workers" leem o mesmo, owner B não recebe nada; 3) v2 com `effective_from` futuro — turn em andamento continua lendo v1; 4) turn seguinte (relógio avançado) lê v2, trace via `tracing::info!` + query de auditoria (`RuleStore::latest_published`) confirmam a versão; 5) rollback pra v1 publicado como v3 NOVA (histórico de 3 entradas preservado, nunca reescrito); 6) "worker" que ficou offline durante v2 recupera v3 correto no próximo read (sem cache per-worker desatualizado); 7) regra crítica possivelmente stale segue a policy declarada (`FailClosed` omite o bloco; `UseLastKnown` serve a última revisão conhecida mesmo assim) — provado com dois owners/policies distintos.

**Dois owners, zero cross-owner (M5-02):** um único `AgentLoop`, `owner_a`/`owner_b` via `run_turn_for` — sessões CR-04 diferentes (`SessionManager::load_most_recent_id_for` confirma ids distintos), histórico de cada owner só contém sua própria mensagem, contexto autoritativo (component 2) e RuleBundle (M5.1) nunca vazam entre owners, e nenhuma entidade/objeto do host aparece no histórico persistido (`session.load_recent`) — o estado autoritativo do host é 100% in-process, nunca chega ao SQLite do Bastion.

Gates verdes: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --workspace` (730 passed / 10 ignored — 57 suites, +1 suite = `embedded-host-slice`), `check-crate-deps.sh` PASS (zero crate nova em `crates/` — o slice é um `examples/*`, fora do escopo desse script, verificado manualmente), `dump-public-api.sh --check` PASS (só `bastion-types` mudou — 9 itens novos, todos aditivos: `ContextRevision`/`StalePolicy`/`VersionedContextArtifact` + métodos), `check-scope-and-scrub.sh` PASS (zero símbolo de nuvem/tenancy, zero nome corporativo — slice lido como host embedded genérico).

## Encerramento do Loop 3-D (2026-07-14)

Quatro commits sobre `origin/revamp` (`226ce40`, `78cee0d`, `459ba31`, `7a91e32`), fechando o contrato cloud-ready (`docs/revamp/C3-cloud-ready-design.md`) — M4-14 e M4-15 do BACKLOG. Regra-mãe verificada em cada commit: nenhum conceito de nuvem (billing/marketplace/tenancy/control-plane) entrou no kernel; tudo aqui é o contrato operacional neutro + os dois pontos de segurança inegociáveis.

- **`feat(c3): SecretRef + SecretResolver`** — estende o padrão `AuthProfileRef`/`AuthResolver` (Loop 3-B) pra secrets de config do daemon. `bastion-types::secret` ganha `SecretRef` (nome, nunca valor), `SecretValue` (Debug/Display sempre redigidos — `expose_secret()` é o único escape hatch, e não implementa `Serialize`, então não pode ser serializado por acidente em export/dump algum), `SecretResolver` (trait, resolução injetável) e `NullSecretResolver` fail-closed. App-level (`src/secret.rs`): `EnvSecretResolver`, `MountedFileSecretResolver` (diretório montado — o formato que um Secret volume do Kubernetes ou um bind-mount de operador produz), `LayeredSecretResolver` (env primeiro, depois `BASTION_SECRETS_DIR` opcional — comportamento idêntico a antes quando essa var não existe). `APP_JWT_SECRET`/`BASTION_INFER_TOKEN` em `main.rs` passam a resolver por essa via em vez de `std::env::var` cru. O gap documentado desde o M4-08 (`manifest.secrets` do protocolo de extensão nunca resolvia pra valor real) fica fechado: `SubprocessCapability` agora resolve cada `SecretRef` declarado e injeta SÓ esses no env do filho após `env_clear()` — fail-closed se um secret declarado não resolver.
- **`feat(c3): daemon operational contract`** — `/healthz` (liveness, sempre 200) e `/readyz` (readiness, 200 só quando `ReadinessState` reporta session+memory+provider+channels prontos — os 3 primeiros ficam prontos no início do `daemon_loop`, já garantidos por construção; `channels` só depois de todo canal configurado terminar sua tentativa de spawn) montados no MESMO axum router do webhook. `POST /lifecycle/stop` dispara o mesmo caminho de shutdown gracioso do SIGTERM/Ctrl-C via um `tokio::sync::Notify` compartilhado; `POST /lifecycle/reload` recarrega o `PersonaRegistry` usado por `/as`/`/cabinet` (honestamente escopado: não hot-swapa o registry do `PersonaResponder`, que exigiria um port novo no kernel). Ambos gateados por `DaemonAccessAuth` — token resolvido via o `SecretResolver` do commit anterior (`BASTION_DAEMON_TOKEN`), fail-closed quando não configurado. `.af` (INTEROP-01) ganha campo `producer` (default-populated — nenhum `.af` legado quebra); `version` já servia de schema_version desde sempre e fica sem renomear. **Achado reportado, não alterado:** export `--with-identity` embute o keypair age/Ed25519 em texto puro por design (é a própria identidade portável, não um secret-por-referência — já endurecido: opt-in, chmod 0600 no write). Dois testes novos fixam a fronteira: o path comum (sem `--with-identity`) não vaza NADA secret-shaped; o path opt-in tem a exceção deliberada, testada explicitamente em vez de escondida.
- **`feat(c3): extension UI isolation (CLD-08)`** — `Provided::Ui` (declarado no Loop 3-C, nunca servido) ganha mecanismo: `ExtensionUiHost` serve os assets registrados de uma extensão com `Content-Security-Policy: sandbox allow-scripts` (deliberadamente SEM `allow-same-origin` — é isso que força um browser compliant a tratar a resposta como documento de origem opaca, sem acesso ao DOM/cookies/storage da UI host mesmo que o script tente) + `nosniff`/`frame-ancestors` em profundidade. Único canal de volta ao backend é `POST /{id}/invoke`, mediado pelo MESMO `CapabilityRegistry` e gateado pelo `PermissionSet` daquela extensão — capability fora do declarado é `ExtensionError::CapabilityNotDeclared` tipado, nunca alcança o registry real. Suíte adversarial nova (`tests/extension_ui_adversarial.rs`, estende o estilo de `tests/extension_adversarial.rs`) prova os 2 vetores pedidos: (a) cabeçalho de isolamento presente e sem `allow-same-origin`; (b) invoke fora do PermissionSet bloqueado. Não há cockpit web rico neste repo pra plugar um browser real — mecanismo entregue, wiring num consumidor de produto é follow-up (mesmo padrão "mecanismo agora, UX de produto depois" do M4-09/M4-10).
- **`feat(c3): reproducible container + scrub guard`** — `tests/boot_local_and_hosted.rs` prova o MESMO binário de teste compilado bootando modo "local" (paths/secret via env) e "hosted-like" (paths diferentes + secret via `BASTION_SECRETS_DIR`) sequencialmente no mesmo processo — a prova mais forte disponível de "sem recompilar" nesse nível. `docker build` real NÃO foi executado (disco do sandbox apertado, flagado explicitamente no brief da tarefa; nem `buildx`/`hadolint` disponíveis pra lint barato) — revisão estática confirmou que o Dockerfile já não bake nenhum path/secret (paths via `bastion.toml` + override `BASTION__*`, secrets via `.env`/`BASTION_CONFIG`); comentário novo documenta o trade-off `/healthz`-sobre-HTTP vs. sem `HEALTHCHECK` in-container (scratch não tem shell — restrição pré-existente, inalterada). `scripts/check-scope-and-scrub.sh` (novo job de CI `scope-and-scrub`) bloqueia símbolo de billing/marketplace/tenancy/control-plane em código compilado (nunca em docs/, nunca em comentário) + nome corporativo em qualquer arquivo tracked — os dois verificados disparando de fato contra violação sintética, não só passando na árvore atual.

Gates verdes em cada commit: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --workspace` (723 passed / 10 ignored ao final — 56 suites), `check-crate-deps.sh` PASS (zero crate nova), `dump-public-api.sh --check` PASS (baselines `bastion`/`bastion-types`/`bastion-mesh` regenerados).

**Impasses/achados reportados ao owner, não escondidos:**
- **BACKLOG.md numera o design doc "M4-13..15" mas o conteúdo do doc só cobre M4-14 (contrato cloud-ready) e M4-15 (UI isolada)** — M4-13 no BACKLOG é discovery híbrido (catálogo de extensões, decisão #12), um tema DIFERENTE que `C3-cloud-ready-design.md` não aborda em nenhuma seção. Este loop fecha M4-14/M4-15; M4-13 permanece `[ ]` — não marcado como feito, discrepância de numeração sinalizada aqui em vez de resolvida por adivinhação.
- **Export `.af` com `--with-identity`** continua embutindo o keypair em texto puro — ver acima, decisão de não mudar o formato (identidade portável ≠ secret-por-referência), documentada e testada, não silenciosa.
- **`aag impact`/`aag affected --stdin` agregado no fim do loop bateu "database is locked"** (index provavelmente sendo escrito por outro processo) — impact analysis por símbolo já tinha sido feito em cada edição individual (mandato do AGENTS.md cumprido incrementalmente); o agregado de fechamento não pôde ser re-confirmado por essa causa, registrado aqui em vez de bloqueado.
- **`docker build` real não executado** — ver acima; recomendado rodar em CI (disco fresco) como primeira validação real de build de imagem.
- **`POST /lifecycle/reload` não hot-swapa o registry do `PersonaResponder`** (só o `command_resources.registry` usado por `/as`/`/cabinet`) — mudar isso exigiria um port novo no kernel (`Responder`-level hot-reload), fora do escopo deste loop; logado explicitamente quando acionado, não fingido.

## Encerramento do Loop 3-C (2026-07-14)

Cinco commits sobre `origin/revamp`, fechando M4-08..12 (`docs/revamp/C3-extension-protocol-design.md`): protocolo + host + os 3 mecanismos com uma extensão de referência por kind + suíte adversarial + pack de referência.

- **`feat(c3): bastion-extension-protocol crate`** — `ExtensionManifest`/`PackManifest`/`PermissionSet`/trust tiers/`ExtensionError`/`LoadoutLock`, contratos-only, dep só `bastion-types`. `PermissionSet` carrega a lógica pura de decisão (`allows_capability`/`allows_egress_host`/`allows_memory_read`/`allows_network_bind`/`is_subset_of`) que tanto o host quanto a suíte adversarial consultam — deny-by-default em todo campo, sem `_ => false` catch-all (troca de variante força recompilar todo call site).
- **`feat(c3): extension host + declarative + subprocess mechanisms`** — `src/extension/` (app, fora do kernel): `HostFacade` é o ÚNICO chokepoint que um mecanismo atravessa pra registrar capability/alcançar host/ler memória/abrir socket — espelha `CapabilityRegistry::invoke`'s "uma fronteira de política" uma camada antes. `ExtensionHost::install`/`upgrade`/`rollback`/`revoke` confiam só no que `HostFacade::registered_capabilities()` reporta ter realmente registrado (nunca no que o manifest afirma) pra rollback atômico. Mecanismo `Subprocess`: processo separado, `env_clear()` (sem allowlist ainda — filho não herda NADA além de argv/stdio), protocolo NDJSON versionado onde toda pergunta cross-boundary (egress fetch, memory read, network bind, até "registra uma capability") é respondida pelas MESMAS checagens de `PermissionSet` que `HostFacade` usa — registrar capability mid-invoke é adicionalmente IMPOSSÍVEL por construção (`invoke()` nunca segura um `CapabilityRegistry`).
- **`feat(c3): Wasm mechanism (bastion-extension-wasm, wasmi-backed)`** — **achado de engenharia real:** `wasmtime` compilou limpo isolado (30s/1.4GB), mas ligado no app puxou cranelift+~207 crates transitivos pra dentro de CADA um dos ~20 binários de teste do workspace — `cargo test --workspace` bateu `ld terminated with signal 7 [Bus error]` (ENOSPC) repetidamente, mesmo logo após `cargo clean` completo. Trocado por `wasmi` (interpretador puro-Rust, sem JIT/cranelift/LLVM, ~30 crates transitivos): build+test completo do workspace sobra com 12GB+ de folga. Mesma propriedade de segurança nos dois: um `Linker` vazio não dá NENHUMA autoridade ambiente ao guest (nem WASI), e ambos suportam fuel metering. Extensão de referência (`add`/`busy_loop`, `#![no_std]`, 129 bytes) prova execução real E que fuel bounds um guest hostil/travado em vez de travar o daemon.
- **`test(c3): adversarial extension suite`** — `tests/extension_adversarial.rs`: os 4 vetores (a/b/c/d) do §8.3, cada um via `ExtensionHost::install` real (não facade nua), erro tipado + zero órfão (a capability legítima que o mesmo `activate()` registra ANTES da tentativa má também é revertida); §8.4 pack não amplia autoridade (membro ganancioso bloqueado por `resolve_pack` antes de qualquer install); §8.5 duas rotas de bloqueio de upgrade (compat-range de protocolo E requires de um dependente), ambas com loadout intocado.
- **`feat(c3): reference pack (real-usage, dogfood)`** — `tests/extension_reference_pack.rs`: pack `mario/life-os-developer-pack` compondo os 3 kinds (Declarative: prompt diário; Subprocess: trigger de repo via `reference-extension-echo` real; Wasm: cálculo de orçamento) — ciclo completo install→review de permissão (`src/extension/review.rs`, M4-09)→Loadout resolvido→execução dos 3→upgrade→rollback→revoke, zero órfão. Segundo teste prova que o pack não amplia autoridade de um 4º membro hipotético mais ganancioso.

Gates verdes em cada commit: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --workspace` (670 passed / 10 ignored ao final — 54 suites), `check-crate-deps.sh` PASS (2 crates novas: `bastion-extension-protocol` zero deps além de `bastion-types`, `bastion-extension-wasm` zero deps `bastion-*`), `dump-public-api.sh --check` PASS.

**Fora de escopo, decisão deliberada (§7 da spec):** registry/catálogo público, marketplace, monetização, subagente/agente coletivo (M4-11 — não pedido pelos commits deste loop). **Follow-ups documentados, não silenciosos:**
- Subprocess sem allowlist de env (`SecretRef`-por-nome ainda não resolve pra valor real no ambiente do filho — filho recebe zero env por padrão, seguro mas sem capacidade de usar segredos ainda).
- `trust_tier_of` sempre retorna `Local` — verificação real de assinatura (`ed25519-dalek` já é dep do workspace) fica pro discovery híbrido (M4-13).
- `ExtensionHost` não está fiado no `main.rs`/daemon de produção ainda (nenhum comando `/extensions` de UX) — existe e é testado como subsistema completo e pronto pra essa integração, mas a integração em si é um passo de composição do app, não deste loop de protocolo+mecanismos.

## Encerramento do Loop 3-B (2026-07-14)

Três commits sobre `origin/revamp`, fechando o M4-07 (login por assinatura como backend de primeira classe) e a UX/matriz que faltavam do M4-06:

- **`fix(c3): configurable acpx --auth-policy (unblock opencode)`** — `--auth-policy` deixou de ser hardcoded `"fail"` em `AcpxAgentRuntime`; virou campo por-agente (`default_auth_policy_for`), `"skip"` pra `opencode`, `"fail"` inalterado pro resto. Re-run ao vivo de `acpx_live_opencode.rs`: **8 passed, 5 skipped, 1 failed** (era 5/5/4 bloqueado) — `happy_path` agora PASSA de verdade. O 1 failed remanescente (`artifact_digest`) é um achado NOVO, não-relacionado ao auth (frame shape do opencode que `FrameInterpreter` ainda não reconhece) — documentado em A-05 §2A, fora do escopo deste fix. A-05 §1/§2A atualizados com o placar real.
- **`feat(c3): subscription-backed auth as first-class backend (M4-07)`** — `AuthProfileRef` era threaded mas nunca resolvido de verdade (nem `acpx` nem `codex_app_server` leem `SessionSpec::auth`). Fecha o gap com uma porta nova no kernel (`AuthResolver`, `bastion-runtime/agent/ports.rs`), chamada nos 3 pontos de `start`/`resume` (`run_runtime_backed_turn`, `delegate_task`, `resume_delegated_task`) — default `NullAuthResolver` sempre `Ok` (comportamento idêntico a antes, já que nenhuma checagem existia). App-level `AuthProfileRegistry` (`src/auth_profile_registry.rs`) resolve `[auth.<profile>]` do bastion.toml por REFERÊNCIA — perfis `host-cli` rodam o "whoami" read-only do próprio CLI (`claude auth status`/`codex login status`/`opencode auth list`), inspecionando só o exit code, nunca token; perfis `api-key` checam só se a env var está SET. Critério de aceite do M4-07 provado AO VIVO: turn runtime-backed completo com ZERO env vars `*_API_KEY` no processo (`tests/agent_runtime_backend_live.rs::m4_07_subscription_backend_works_without_api_key_live`). Teste companheiro prova o fail-closed: `AuthProfileRef` não configurado derruba o turn ANTES de qualquer sessão de harness abrir.
- **`feat(c3): backend selection UX + versioned support matrix`** — `/backends` (lista todo backend conhecido + saúde ao vivo + coverage + auth resolvido) e `/backend use <id>|model|task:<id>|task:none` (troca ao vivo, sem restart, fail-closed — id não resolvível NUNCA aplica troca parcial nem cai silenciosamente pra `model`) via o MESMO mecanismo de cockpit command que `/goals`/`/drift`/`/memories` já usam — reuso, não superfície nova. `docs/SUPPORT-MATRIX.md` (público, versionado): tabela alvo × auth × capabilities × policy-coverage, derivada dos `RuntimeDescriptor` reais + placares vivos da A-05, com nota explícita de que limites/ToS pertencem ao adapter/provider, nunca promessa eterna do Core.

Gates: `cargo fmt --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace` verdes em cada commit (611 passed / 10 ignored ao final — até 46 suites), `check-crate-deps.sh` PASS, `dump-public-api.sh --check` PASS (baselines `bastion`/`bastion-runtime`/`bastion-agent-runtime` regenerados a cada commit que mudou pub surface).

M4-07 fica **[~] em progresso**, não `[x]`: falta o adapter Cursor (ACP, nunca implementado) e `codex` via `acpx` segue indisponível (A-05 §4, login-mode mismatch externo). M4-06 fica **[~]**: só falta login guiado/OAuth interativo (fora de escopo por design — ver acima) e `task_runtime` como tool exposta ao modelo.

## Encerramento do Loop 2 — Ciclo 2 completo (2026-07-14 ~04:30)

Ciclo 2 (2.1→2.4) fechado, origin/revamp em `e81c313`, **50 commits sobre a tag**, **589 testes / 42 suites** verdes.

- **2.1** approval vira port (`ApprovalGate`), rejeição tipada (`ApprovalDenied`), `DenyScope::Turn` fail-closed contra route-around-deny, trust parity nos bypass paths. Bug latente de bônus: `SessionManager::append` rejeitava 2º tool_result de round multi-tool — corrigido.
- **2.2** contrato `AgentRuntime` v2: WATCHDOG parametrizável, `SandboxCoverage` DETECTADO (bubblewrap probe — confirmou `None` na máquina do owner, não mais `Partial` otimista), `ResumeSpec`, `PermissionDecision::Deny{scope}`.
- **2.3** 19 shims do M2 removidos, superfície pub −26, `VERSIONING.md` + `dump-public-api.sh` baseline no CI.
- **2.4** `BackendProfile` + `RuntimeRegistry` integrados no daemon (default `Model` = comportamento idêntico); modo 2 (runtime-backed conversation) e modo 3 (delegated task) com **A-06 e A-07 provados LIVE** (Claude Code / Codex reais). Harness nunca implementa `Provider` (verificado).

### Pendências do owner (bloqueiam o próximo ciclo autônomo)

1. **M5 — scoping do segundo consumidor (host embedded):** o `embedded-host` de exemplo prova a API, mas o slice M5 real (contexto autoritativo injetado, action nomeada, policy fechada, 2 owners) precisa de decisão de escopo — o que ele representa sem violar o scrub público.
2. **A-08** security/live E2E + matriz de policy coverage versionada; **A-09** remoção do terminal-agent legado (gate: A-08 verde + rollback testado).
3. ~~**opencode auth login** — destrava a célula que falta na matriz A-05.~~ ~~**LOGIN FEITO / célula preenchida, resultado BLOQUEADO.**~~ **RESOLVIDO (Loop 3-B).** Login destravou o handshake ACP, mas expôs um blocker distinto e de nível de adapter: `AcpxAgentRuntime` fixava `--auth-policy fail` pra todo agente; o servidor ACP nativo do opencode anuncia `authMethods` que o matcher de credenciais do próprio acpx não reconhece (opencode guarda credenciais em store separado), então abortava antes de `session/prompt` sair. Fix real do Loop 3-B: `--auth-policy` virou campo por-agente do adapter (`default_auth_policy_for` em `src/acpx.rs`) — `"skip"` pra `opencode`, `"fail"` (inalterado) pra todo o resto, incluindo `claude`. Re-run ao vivo confirma: `happy_path` e mais 7 checks agora PASSAM (eram FAIL pelo abort de auth); sobra 1 achado NOVO e não-relacionado (`artifact_digest` — `FrameInterpreter` não reconhece o frame shape de tool-call do opencode, documentado em A-05 §2A, fora de escopo deste fix). Placar real: **8 passed, 5 skipped, 1 failed** (era 5/5/4 com o bloqueio).
4. **3 itens `?` do inventário M0** (dir `bastion/` tracked, `.bastion/`, destino do `STRATEGY.md`).
5. **M4 pleno:** ~~aprovação cross-turn genuína (achado 6a)~~ **RESOLVIDO (Loop 3-A)**, ~~owner-routing do `pending_tx` (6d)~~ **RESOLVIDO (Loop 3-A)**, ~~regex `pub async fn` do baseline (6c)~~ **RESOLVIDO (Loop 3-A)**, ~~seleção de backend por UX, matriz de assinatura versionada~~ **RESOLVIDO (Loop 3-B, ver seção abaixo)** — só **6b** (continuidade de task cross-restart, furo de protocolo do harness, fora de alcance do kernel) e **login guiado/OAuth interativo** (Bastion não realiza login, só verifica um login de CLI já feito pelo owner — fronteira de segurança deliberada) seguem em aberto pro M4 pleno.
6. **M4-08..12 (protocolo de extensões) RESOLVIDO (Loop 3-C, ver seção abaixo)** — protocolo + host + os 3 mecanismos (Declarative/Subprocess/Wasm) + suíte adversarial + pack de referência, todos com gates verdes. Follow-ups documentados (não bloqueantes pro próximo ciclo, mas decisão do owner sobre priorizar): (a) `main.rs`/daemon ainda não fia `ExtensionHost` na composição de produção nem expõe um comando `/extensions` — o subsistema existe e está testado ponta a ponta, falta só o passo de composição do app; (b) mecanismo `Subprocess` não tem allowlist de env/segredos ainda (filho recebe zero env por padrão — seguro, mas sem capacidade de usar `SecretRef`s de verdade); (c) `review::trust_tier_of` sempre retorna `Local` (verificação real de assinatura fica pro M4-13/discovery híbrido); (d) engine do mecanismo Wasm é `wasmi` (interpretador), não `wasmtime` — troca documentada no commit por ENOSPC real neste ambiente de build, decisão reversível se o ambiente ganhar mais disco.

### Próximo ciclo sugerido (quando o owner destravar 1-4)

M4 pleno (BackendProfile UX + login) → M5 (slice segundo consumidor) → M6 (split físico bastion-core/bastion-agent + limpeza geral GSD/.planning/mortos) → M7 (validação competitiva). Ordem do BACKLOG mantida.

---

## Encerramento do loop (2026-07-14 ~00:15)

Entregue nesta corrida: M0, M1 completo, M2 completo (9 crates + app + CI deps), M3 estático (F1 hardening, invariantes doc, exemplos, feature flags, build mínimo 15,6MB), Trilha A até A-05 (contrato, conformance, 2 adapters com validação LIVE, matriz). 537 testes / 40 suites verdes; origin/revamp atualizado a cada marco.

**Fila do próximo ciclo (em ordem):** (1) ~~fix dos furos de approval (finding #3) — pré-requisito do M5~~ **RESOLVIDO (Ciclo 2.1)**; (2) ~~revisão do contrato A-01 com os 6 furos do finding #5~~ **RESOLVIDO (Ciclo 2.2)**; (3) ~~M4 (BackendProfile, A-06/A-07 dependem dele)~~ **RESOLVIDO (Ciclo 2.4, `7726ad9`/`8b2cae4`/`c69a810`)** — `BackendProfile`/`ConversationBackend`/`RuntimeRegistry` no kernel (default = Model, comportamento preservado), modo 2 (runtime-backed conversation) e modo 3 (delegated task) integrados em `AgentLoop`, A-06 e A-07 validados AO VIVO (`docs/revamp/A-06-A-07-live.md`); UX rica/login guiado/task_runtime-como-tool seguem M4 pleno (ver BACKLOG.md M4-06); (4) M3 restante (semver/compat tests/redução de pub/remoção dos 19 shims); (5) células faltantes da matriz A-05 (opencode auth login + codex-via-acpx) — **opencode preenchida** (bloqueada por `--auth-policy fail` do adapter vs auth do opencode, ver Pendência #3 acima e A-05 §2A), **codex-via-acpx segue faltando**. Itens que precisam do Mario: decisão sobre deny-escalation (finding #5.5, já resolvida no design pelo Ciclo 2.1/2.2 — ver acima); 3 itens `?` do inventário M0.

## Decisões operacionais do loop

- Reindex GitNexus adiado pro fim do M2 (extração invalida o índice a cada `git mv`); `detect_changes` roda mas é sinal fraco até lá.
- Padrão de falha recorrente: subagentes encerram deixando cargo em background (3 ocorrências) — mitigado com fingerprint + wakeup de estagnação + finalização inline.
- Máquina do owner caiu 2x com builds pesados — todos os builds em `CARGO_BUILD_JOBS=2`, um por vez.
- ENOSPC durante `cargo test` do 3b — liberados 23G (`target/debug/incremental` + deps antigos). Recorrente nesta máquina; se voltar, mesmo remédio.
- Session limit da API derrubou o 3b no meio do git mv (reset 19:20); retomado por SendMessage sem perda — renames staged sobreviveram.

## Pendências pro owner

- Inventário M0-03 tem 3 itens `?`: diretório `bastion/` tracked, `bastion.local.toml`/`.bastion/`, destino do `STRATEGY.md`.
- Login Codex/ChatGPT necessário pra validação live do `CodexAppServerRuntime` (A-03); acpx→Claude Code tentará validação com a auth local existente.
